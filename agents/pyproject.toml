[project]
name = "nuncio-agents"
version = "0.1.0"
description = "Band of Agents: collaborative multi-agent sales personalization for nuncio"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "band-sdk>=1.0.0",
    "httpx>=0.27.0",
]
